version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - revive
    - bodyclose
    - misspell

  settings:
    revive:
      rules:
        - name: exported
          disabled: true
